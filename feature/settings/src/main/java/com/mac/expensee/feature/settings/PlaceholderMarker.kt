package com.mac.expensee.feature.settings

/**
 * This module is scaffolded (Gradle module + dependencies wired) but its feature code is not
 * implemented yet -- see project README "Implementation phases". Deferred deliberately rather
 * than stubbed with fake screens, so Phase 1 stays reviewable as "auth end-to-end, working".
 */
internal object PlaceholderMarker
